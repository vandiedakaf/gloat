#
# vandiedakaf solutions 2016
#

flyway.schemas: gloat

logging:
  level:
    org.springframework.web: INFO
    com.vdda: DEBUG

spring:
  datasource:
    url: jdbc:mysql://localhost/mysql
    username: root
    password:
    driver-class-name: com.mysql.jdbc.Driver
    testOnBorrow: true
    validationQuery: SELECT 1
